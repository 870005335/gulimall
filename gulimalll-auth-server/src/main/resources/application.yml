spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gulimall-auth-server
  redis:
    host: 192.168.1.96
  session:
    store-type: redis

server:
  port: 20000


logging:
  level:
    com.alibaba.nacos.client.config.impl: warn